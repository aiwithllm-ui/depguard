# Contributing

Please discuss changes that alter sandbox boundaries, privacy guarantees, evidence schemas, or exit codes before implementation. Keep package metadata facts, observations, and policy conclusions separate.

Run `npm test` and `npm run lint` before submitting a change. Fixture repositories must be self-contained and must not make network requests.
